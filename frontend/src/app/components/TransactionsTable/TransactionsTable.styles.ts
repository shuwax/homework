import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  tableContainer: {
    width: "100%",
  },
  buttonsTableCell: {
    padding: 5,
    width: 100,
    minWidth: 100,
  },
  tableRowContainer: {
    cursor: "pointer",
  },
}));
