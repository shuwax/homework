export interface GetParamsInterface {
  pagination?: boolean;
  page?: number;
  showAvailableProducts?: boolean;
  process?: Array<number>;
  onlyRegularUsers?: boolean;
  visibility?: boolean;
  showArchivedRoom?: boolean;
}
